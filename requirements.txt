# Flask framework and WSGI utilities
Flask>=2.2.0,<3.0.0 # Updated version range
Werkzeug>=2.2.0,<3.0.0 # Updated version range matching Flask

# Firebase Admin SDK
firebase-admin>=6.1.0,<7.0.0 # Updated version range

# Google Cloud Vertex AI SDK
# Check for the latest stable version compatible with your needs
vertexai>=1.40.0,<2.0.0 # Example: Use a recent stable version
google-api-core>=2.10.0,<3.0.0 # Often needed for exceptions like ResourceExhausted

# WSGI Server for production
gunicorn>=21.2.0,<22.0.0 # Updated version range

# Google Cloud Secret Manager Client
google-cloud-secret-manager>=2.16.0,<3.0.0 # Seems relatively recent, check if update needed

# python-dotenv for local development environment variables
# Consider removing from production build if env vars are injected externally
python-dotenv>=1.0.0,<2.0.0 # Updated version range